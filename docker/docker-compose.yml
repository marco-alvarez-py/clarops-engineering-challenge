services:
  challenge-postgresql:
    env_file:
      - .env
    image: bitnamilegacy/postgresql:15.4.0-debian-11-r56
    ports:
      - "${PG_HOST_PORT:-5432}:5432"
    networks:
      - "${DOCKER_NETWORK_NAME:-clarops_challenge_network}"
    volumes:
      - postgresql_data:/bitnami/postgresql
      - ./init-scripts/db:/docker-entrypoint-initdb.d
    environment:
      - POSTGRESQL_USERNAME=${PG_USERNAME:-clarops}
      - POSTGRESQL_PASSWORD=${PG_PASSWORD:-hOTz05r9ul}
      - POSTGRESQL_DATABASE=${PG_DATABASE:-clarops_challenge}
      - POSTGRESQL_POSTGRES_PASSWORD=${PG_ADMIN_PASSWORD:-Tcawt5xK07}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${PG_USERNAME:-clarops} -d ${PG_DATABASE:-clarops_challenge} -h localhost -p 5432" ]
      interval: 3s
      timeout: 2s
      retries: 60
      start_period: 10s

volumes:
  postgresql_data:
    driver: local

networks:
  clarops_challenge_network:
    driver: bridge
